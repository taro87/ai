1장 주피터노트북 사용협
	-shift+enter
	-esc+b, a, m
2장 자료형과 연산자
1. 변수
	선언X, 할당과 동시에 타입 설정
	문자, _, 숫자 사용가능. 문자로 시작
	예약어 사용불가.
	다중 변수 할당 가능(일괄 할당)
2. 화면i/o
	input(lable) : 사용자부터 문자열 입력
	print(출력내용들, sep='구분자', end='종결어구')
3. 기본자료형
	숫자 : int, float, complex(복소수)
		int/int => 실수 / 지수형표현 ex1.12e-20
	문자 : +,*연산
		정규표현식 : re.findall(), re.search()